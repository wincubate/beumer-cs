using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Xml.Linq;

namespace Wincubate.Module14.Slide30
{
   class Program
   {
      static void Main( string[] args )
      {
         // Insert LINQ to XML here...

         //XDocument doc = XDocument.Load( @"..\..\movies.xml" );

         //var query = from e in doc.Descendants( "Movie" )
         //            where e.Attribute( "Type" ).Value == "Comedy"
         //            select e.Element( "Title" ).Value;

         //foreach ( var movie in query )
         //{
         //   Console.WriteLine( movie );
         //}
      }
   }
}
